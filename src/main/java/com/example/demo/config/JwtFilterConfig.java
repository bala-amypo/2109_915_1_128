package com.example.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtFilterConfig {
    // Configuration class for JWT filter registration - already handled in SecurityConfig
}
